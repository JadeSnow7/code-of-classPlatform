[package]
name = "simulation-rs"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "simulation_rs"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.23", features = ["abi3-py39", "extension-module"] }
