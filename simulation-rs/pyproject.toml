[build-system]
requires = ["maturin>=1.5,<2"]
build-backend = "maturin"

[project]
name = "simulation-rs"
version = "0.1.0"
requires-python = ">=3.9"

[tool.maturin]
module-name = "simulation_rs"
features = ["pyo3/extension-module"]
